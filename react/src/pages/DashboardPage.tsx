import React from 'react'

export default function DashboardPage() {
  return (
    <div className="card">
      <h2>Dashboard</h2>
      <p>Admin / Teacher tools will migrate here (metrics, post moderation, contest management).</p>
    </div>
  )
}
